# 複製為 .env 並填入實際值（.env 已被 .gitignore 忽略，不會進版控）

# 前端代理（webapp/app.py）指向的上游查詢 API
PAIR2_API_URL=http://your-api-host:8000
PAIR2_API_TIMEOUT=110

# 查詢 API（pair2api）存取 HDFS 的使用者
HADOOP_USER_NAME=your_hdfs_user

# Spark 查詢併發上限（避免 driver heap 過載）
MAX_CONCURRENT_SPARK_QUERIES=4
